# AGENTS.md

## Cursor Cloud specific instructions

This repository (`OWBanPickwebsite`) is currently an empty/newly initialized repository with only an empty `README.md` file. There is no application code, no dependencies, no build system, and no services to run.

### Current state (as of initial setup)

- **No package manager files** (no `package.json`, `requirements.txt`, `Cargo.toml`, etc.)
- **No source code** in any language
- **No tests, linting, or build configuration**
- **No Docker or container configuration**
- **No CI/CD pipelines**

### When code is added

Once code is added to this repository, future agents should:
1. Re-evaluate the project structure and identify the tech stack
2. Install appropriate dependencies based on lockfiles/manifests
3. Update this section with relevant development instructions
